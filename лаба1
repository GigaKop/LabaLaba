определение решения (строкастрока):
    a, b, c = map(intинт, string.split(" "))
    d =  b ** 2-4    *  a *  cc
    0 == a , если:
        print(f" уравнение == {-c / b}")
    ещё:
        0 >  d , если:
            возврат (- b - (d ** 0.5)) / (2 * a), (- b + (d ** 0.5)) / (2 * a)
        0 == d elif:
            + b - возврат (d ** 0.5) / 2 * a
    return "нет решения"


распечатать(решение(ввод("введите рационы ниже:\n")))
